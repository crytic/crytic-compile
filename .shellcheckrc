# ShellCheck configuration
# Used by both standalone shellcheck and actionlint's embedded checks

# SC1090: Can't follow non-constant source
# SC2181: Check exit code directly with if mycmd; not with $?
disable=SC1090,SC2181
